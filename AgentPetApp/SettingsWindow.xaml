<Window x:Class="AgentPetApp.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AgentPet Settings" Height="600" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType"
        Foreground="White">
    <Grid Margin="10">
        <!-- Shadow Layer -->
        <Border Background="Black" CornerRadius="12">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.4"/>
            </Border.Effect>
        </Border>
        
        <!-- Content Layer -->
        <Border Background="#2B1A3F" CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="#3E2254" CornerRadius="12,12,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="AgentPet Settings" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    <Button Content="✕" Width="40" Background="Transparent" Foreground="White" HorizontalAlignment="Right" Click="CloseButton_Click"/>
                </Grid>
            </Border>
            
            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="10">
            <TabItem Header="General">
                <StackPanel Margin="20">
                    <TextBlock Text="Sounds" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="When an agent finishes" Foreground="White" VerticalAlignment="Center"/>
                        <CheckBox HorizontalAlignment="Right" IsChecked="True" VerticalAlignment="Center"/>
                    </Grid>
                    <Grid Margin="0,0,0,20">
                        <TextBlock Text="When an agent needs input" Foreground="White" VerticalAlignment="Center"/>
                        <CheckBox HorizontalAlignment="Right" IsChecked="True" VerticalAlignment="Center"/>
                    </Grid>
                    
                    <TextBlock Text="Window" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Enable Drag Mode (move pet around)" Foreground="White" VerticalAlignment="Center"/>
                        <CheckBox x:Name="DragModeCheck" HorizontalAlignment="Right" VerticalAlignment="Center" Click="DragModeCheck_Click"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Show chat bubble" Foreground="White" VerticalAlignment="Center"/>
                        <CheckBox x:Name="ShowChatBubbleCheck" HorizontalAlignment="Right" VerticalAlignment="Center" Click="ShowChatBubbleCheck_Click"/>
                    </Grid>
                    
                    <TextBlock Text="Agent integrations" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,20,0,15"/>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Claude Code" Foreground="White" VerticalAlignment="Center"/>
                        <Button x:Name="ClaudeInstallBtn" Content="Install" Width="80" HorizontalAlignment="Right" Padding="5" Click="ClaudeInstallBtn_Click"/>
                        <Button x:Name="ClaudeRemoveBtn" Content="Remove" Width="80" HorizontalAlignment="Right" Padding="5" Click="ClaudeRemoveBtn_Click" Visibility="Collapsed" Background="#E81123"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Codex" Foreground="White" VerticalAlignment="Center"/>
                        <Button x:Name="CodexInstallBtn" Content="Install" Width="80" HorizontalAlignment="Right" Padding="5" Click="CodexInstallBtn_Click"/>
                        <Button x:Name="CodexRemoveBtn" Content="Remove" Width="80" HorizontalAlignment="Right" Padding="5" Click="CodexRemoveBtn_Click" Visibility="Collapsed" Background="#E81123"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Cursor" Foreground="White" VerticalAlignment="Center"/>
                        <Button x:Name="CursorInstallBtn" Content="Install" Width="80" HorizontalAlignment="Right" Padding="5" Click="CursorInstallBtn_Click"/>
                        <Button x:Name="CursorRemoveBtn" Content="Remove" Width="80" HorizontalAlignment="Right" Padding="5" Click="CursorRemoveBtn_Click" Visibility="Collapsed" Background="#E81123"/>
                    </Grid>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Windsurf" Foreground="White" VerticalAlignment="Center"/>
                        <Button x:Name="WindsurfInstallBtn" Content="Install" Width="80" HorizontalAlignment="Right" Padding="5" Click="WindsurfInstallBtn_Click"/>
                        <Button x:Name="WindsurfRemoveBtn" Content="Remove" Width="80" HorizontalAlignment="Right" Padding="5" Click="WindsurfRemoveBtn_Click" Visibility="Collapsed" Background="#E81123"/>
                    </Grid>
                </StackPanel>
            </TabItem>
            
            <TabItem Header="Pet">
                <StackPanel Margin="20">
                    <Grid Margin="0,0,0,15">
                        <TextBlock Text="Choose pet" FontSize="18" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                        <Button x:Name="ImportPetBtn" Content="Import .zip" Width="80" HorizontalAlignment="Right" Padding="5" Click="ImportPetBtn_Click" Background="#4A2F6E" Foreground="White" BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                    
                    <!-- Pet Grid -->
                    <Border Background="#3E2254" CornerRadius="8" Height="150" Margin="0,0,0,20">
                        <ListBox x:Name="PetsList" Background="Transparent" BorderThickness="0" Margin="10" SelectionChanged="PetsList_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel IsItemsHost="True"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border x:Name="ItemBorder" CornerRadius="8" Padding="10" Background="Transparent">
                                        <StackPanel Width="60">
                                            <Border Width="48" Height="48" CornerRadius="8" Background="#2B1A3F">
                                                <TextBlock Text="🐾" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>
                                            </Border>
                                            <TextBlock Text="{Binding Name}" Foreground="White" TextAlignment="Center" Margin="0,5,0,0" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                    
                    <TextBlock Text="Animations" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
                    <ItemsControl x:Name="AnimationsList" Margin="0,0,0,20">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding MoodName}" Background="#4A2F6E" Foreground="White" Padding="15,5" Margin="0,0,10,10" BorderThickness="0" Cursor="Hand" Click="AnimationPill_Click">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="Size on screen" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,20,0,15"/>
                    <Slider x:Name="SizeSlider" Minimum="64" Maximum="256" Value="128" TickFrequency="64" IsSnapToTickEnabled="True" Margin="0,0,0,20" ValueChanged="SizeSlider_ValueChanged"/>
                    
                    <Grid Margin="0,0,0,15">
                        <TextBlock Text="Animation Speed (FPS)" FontSize="18" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock x:Name="FpsValueText" Text="{Binding ElementName=FpsSlider, Path=Value, StringFormat={}{0} fps}" Foreground="#B09CD0" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                    <Slider x:Name="FpsSlider" Minimum="1" Maximum="60" Value="12" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,20" ValueChanged="FpsSlider_ValueChanged"/>
                </StackPanel>
            </TabItem>
            
            <TabItem Header="About">
                <StackPanel Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="AgentPet for Windows" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Version 1.0.0 (Windows Port)" Foreground="#B09CD0" HorizontalAlignment="Center"/>
                </StackPanel>
            </TabItem>
        </TabControl>
        </Grid>
        </Border>
    </Grid>
</Window>
